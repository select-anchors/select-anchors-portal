import './globals.css'
export const metadata = { title: 'Select Anchors Portal', description: 'Client login & ops' }
export default function RootLayout({ children }) {
  return (<html lang="en"><body>{children}</body></html>)
}