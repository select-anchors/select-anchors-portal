import Providers from "./providers";

export const dynamic = "force-dynamic"; // avoid SSG for auth pages

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
